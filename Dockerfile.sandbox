FROM node:20-bookworm-slim

RUN apt-get update && apt-get install -y \
    git curl sudo ca-certificates ripgrep jq \
    && rm -rf /var/lib/apt/lists/*

RUN npm install -g @anthropic-ai/claude-code

WORKDIR /workspace
USER node